import { describe, expect, test } from "vitest";

describe(
"orchestrator",
() => {

test(
"basic routing",
async () => {

const providers =
[
"openai",
"anthropic",
"gemini"
];

expect(
providers.length
).toBe(3);

}

);

}
);
